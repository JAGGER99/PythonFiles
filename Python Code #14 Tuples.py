# Tuples Demo
# you cant change Tuples you can only get information about them
numbers = (1,2,3)
x = numbers.count(1) #says how many there are 
print(x)
y = numbers.index(3) #finds where it is in the tuple
print(y)